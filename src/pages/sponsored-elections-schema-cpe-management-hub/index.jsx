/**
 * Merged into `campaign-management-dashboard` (CPE workspace + campaign operations).
 * Routes still mount `CampaignManagementDashboard`; this file re-exports for any legacy imports.
 */
export { default } from '../campaign-management-dashboard';
